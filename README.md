# Articoli di astronomia

# Getting Started

## Running locally
1. Clone the repository and made updates as detailed above.
2. type: `bundle exec jekyll serve`
3. Run `bundle install` to install ruby dependencies. If you get errors, delete Gemfile.lock and try again.
4. You should now be able to access the website from `localhost:4000`.
